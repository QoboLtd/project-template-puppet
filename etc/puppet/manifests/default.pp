# Default manifest
# 
# This manifest will run by default, if no other is given.
# Therefor you should probably NOT modify it directly. Instead,
# create other manifests.  This will save you some hair pulling.
node default {
	notice("Running the default manifest. Don't expect much.")
}
